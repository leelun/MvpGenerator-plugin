package ${PACKAGE_NAME};

import android.content.Context;
import dagger.Module;
import dagger.Provides;

#parse("File Header.java")
@Module
public class ${ACTIVITY_NAME}ActivityModule {

    @Provides
    public ${ACTIVITY_NAME}ActivityContract.I${ACTIVITY_NAME}Presenter provide${ACTIVITY_NAME}Presenter() {
        return new ${ACTIVITY_NAME}ActivityPresenter();
    }
}