package ${PACKAGE_NAME};

import dagger.Module;
import dagger.Provides;

#parse("File Header.java")
@Module
public class ${FRAGMENT_NAME}FragmentModule {

    @Provides
    public ${FRAGMENT_NAME}FragmentContract.I${FRAGMENT_NAME}Presenter providePresenterLoader() {
        return new ${FRAGMENT_NAME}FragmentPresenter();
    }
}